name: 'Atlas Copilot'
description: 'Talk to Atlas Copilot.'
branding:
  icon: message-circle
author: 'Ariga'
runs:
  using: node20
  main: index.js
inputs:
  gen-test:
    description: Generate atlas tests for the schema changes.
    required: false