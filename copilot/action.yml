# Generated by go run ./cmd/gen github-manifest; DO NOT EDIT.
name: Atlas Copilot
description: Talk to Atlas Copilot.
branding:
  icon: message-circle
author: 'Ariga'
inputs:
  gen-test:
    description: Generate atlas tests for the schema changes.
    default: "false"
runs:
  using: composite
  steps:
    - id: action
      run: ${{ github.action_path }}/../scripts/shim.sh copilot
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        INPUT_GEN-TEST: ${{ inputs.gen-test }}
