# Generated by go run ./cmd/gen github-manifest; DO NOT EDIT.
name: Migrate Auto Rebase
description: Automatically resolves `atlas.sum` conflicts and rebases the migration directory onto the target branch.
branding:
  icon: database
author: 'Ariga'
inputs:
  base-branch:
    description: |
      The base branch to rebase the migration directory onto. Default to the default branch of the repository.
  dir:
    description: |
      The URL of the migration directory to rebase on. By default: `file://migrations`.
  force-rebase:
    description: |
      When true, skip the merge step and rebase whenever there are dev-only migrations (e.g. out-of-order workflow after merging base and running apply with exec-order non-linear).
    default: "false"
  remote:
    description: |
      The remote to fetch from. Defaults to `origin`.
  working-directory:
    description: Atlas working directory. Default is project root
outputs:
  latest_version:
    description: The latest migration version in the directory after rebase.
  rebased:
    description: Whether migration files were rebased. Either "true" or "false".
runs:
  using: node20
  main: index.js
