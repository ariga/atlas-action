# Generated by go run ./cmd/gen github-manifest; DO NOT EDIT.
name: Migrate Auto Rebase
description: Automatically resolves `atlas.sum` conflicts and rebases the migration directory onto the target branch.
branding:
  icon: database
author: 'Ariga'
inputs:
  base-branch:
    description: |
      The base branch to rebase the migration directory onto. Default to the default branch of the repository.
  dir:
    description: |
      The URL of the migration directory to rebase on. By default: `file://migrations`.
  remote:
    description: |
      The remote to fetch from. Defaults to `origin`.
  resolve-target-url:
    description: |
      Optional database URL to resolve out-of-order migrations. When set, the action will run
      `atlas migrate apply` with `--exec-order non-linear` before rebasing and
      `atlas migrate set` after rebasing to mark the rebased migrations as applied.
  working-directory:
    description: Atlas working directory. Default is project root
runs:
  using: node20
  main: index.js
