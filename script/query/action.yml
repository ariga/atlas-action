# Generated by go run ./cmd/gen github-manifest; DO NOT EDIT.
name: Script Query
description: Run Atlas query scripts on a target database
branding:
  icon: database
author: 'Ariga'
inputs:
  files:
    description: |
      URL(s) of the script files or directories to run. For example: `file://scripts`.
      If not set, Atlas uses the scripts defined in the Atlas configuration file.
  match:
    description: |
      Run only the scripts matching the given regexp.
      For example, `^purge_.*` will only run scripts that start with `purge_`.
      Default is to run all scripts.
  url:
    description: |
      The URL of the target database to run the scripts against.
      For example: `mysql://root:pass@localhost:3306/prod`.
  working-directory:
    description: Atlas working directory. Default is project root
  config:
    description: |
      The URL of the Atlas configuration file. By default, Atlas will look for a file
      named `atlas.hcl` in the current directory. For example, `file://config/atlas.hcl`.
      Learn more about [Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    description: |
      The environment to use from the Atlas configuration file. For example, `dev`.
  vars:
    description: |
      A JSON object containing variables to be used in the Atlas configuration file.
      For example, `{"var1": "value1", "var2": "value2"}`.
outputs:
  output:
    description: |
      The output of the executed scripts, joined by newlines. It contains the values
      emitted by their `output` blocks and the results of their `query` blocks.
  report:
    description: |
      A JSON report of the run, containing the result of each executed script.
runs:
  using: node24
  main: index.js
