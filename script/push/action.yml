# Generated by go run ./cmd/gen github-manifest; DO NOT EDIT.
name: Script Push
description: Push Atlas scripts to the Atlas Registry
branding:
  icon: database
author: 'Ariga'
inputs:
  files:
    description: |
      URL(s) of the script files or directories to push. For example: `file://scripts`.
      If not set, Atlas uses the scripts defined in the Atlas configuration file.
  script-name:
    description: |
      The name (slug) of the script repository in Atlas Registry.
      Read more in Atlas website: https://atlasgo.io/registry.
  working-directory:
    description: Atlas working directory. Default is project root
  config:
    description: |
      The URL of the Atlas configuration file. By default, Atlas will look for a file
      named `atlas.hcl` in the current directory. For example, `file://config/atlas.hcl`.
      Learn more about [Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    description: |
      The environment to use from the Atlas configuration file. For example, `dev`.
  vars:
    description: |
      A JSON object containing variables to be used in the Atlas configuration file.
      For example, `{"var1": "value1", "var2": "value2"}`.
outputs:
  files:
    description: The number of script files that were pushed.
  link:
    description: Link to the scripts in Atlas Registry.
  name:
    description: The name of the script repository that was pushed to.
runs:
  using: node24
  main: index.js
